# План реализации улучшенного UX для раздела "Массовая рассылка"

## Текущая реализация
На данный момент в проекте реализована базовая система рассылки с использованием FSM (Finite State Machine) для создания сообщения, выбора целевой аудитории и подтверждения рассылки.

## Улучшенная структура раздела "Массовая рассылка"

### 1. Основное меню рассылки
```
📢 Массовая рассылка
├── 📝 Создать новую рассылку
├── 📋 История рассылок
├── ⏰ Отложенные рассылки
├── 📊 Статистика рассылок
└── 🔙 Назад
```

### 2. Создание новой рассылки - улучшенный UX поток
```
📝 Создать новую рассылку
├── [Ввод текста или медиа + текст]
├── [Кнопка "Назад"]
├── 👀 Предварительный просмотр
├── ⚙️ Настройка параметров
│   ├── 🎯 Целевая аудитория
│   ├── 📅 Отложенная отправка
│   └── 📊 Тип рассылки
└── 📢 Разослать
```

### 3. История рассылок (Drill-down навигация)
```
📋 История рассылок
├── [Список отправленных рассылок с пагинацией]
│   ├── [Выбор рассылки из истории] → [Детали рассылки]
│   │   ├── 🔄 Повторить отправку
│   │   ├── 📊 Посмотреть статистику
│   │   └── 🔙 Назад к списку
│   └── ...
├── [Пагинация: ← Prev 1/10 Next →]
└── 🔙 Назад в меню рассылок
```

### 4. Отложенные рассылки (Drill-down навигация)
```
⏰ Отложенные рассылки
├── [Список отложенных рассылок с пагинацией]
│   ├── [Выбор рассылки из списка] → [Детали отложенной рассылки]
│   │   ├── 🚀 Отправить сейчас
│   │   ├── 🗑 Удалить
│   │   ├── ✏️ Редактировать
│   │   └── 🔙 Назад к списку
│   └── ...
├── [Пагинация: ← Prev 1/5 Next →]
└── 🔙 Назад в меню рассылок
```

### 5. Улучшенная структура callback data для рассылки (с учетом drill-down навигации)
```python
from aiogram.filters.callback_data import CallbackData

class BroadcastAction(CallbackData, prefix="broadcast"):
    """
    Callback data для рассылки
    action: create, send_now, delete_scheduled, repeat_broadcast, history, scheduled, stats, view_detail, edit, paginate
    broadcast_id: ID of the broadcast (if applicable)
    page: номер страницы для пагинации
    """
    action: str
    broadcast_id: int = 0
    page: int = 1

class BroadcastParams(CallbackData, prefix="bc_params"):
    """
    Callback data для параметров рассылки
    param: target_audience, schedule, broadcast_type
    """
    param: str
    value: str = ""

class BroadcastPagination(CallbackData, prefix="bc_pag"):
    """
    Callback data для пагинации в списках рассылок
    action: prev, next, goto
    page: номер страницы
    section: раздел (history, scheduled)
    """
    action: str
    page: int = 1
    section: str = "history"
```

### 6. Улучшенные клавиатуры для рассылки

#### 6.1. Основная клавиатура рассылки
```python
def broadcast_main_keyboard() -> InlineKeyboardMarkup:
    """Главная клавиатура раздела рассылки"""
    kb = InlineKeyboardBuilder()
    kb.button(text="📝 Создать рассылку", callback_data=BroadcastAction(action="create").pack())
    kb.button(text="📋 История", callback_data=BroadcastAction(action="history").pack())
    kb.button(text="⏰ Отложенные", callback_data=BroadcastAction(action="scheduled").pack())
    kb.button(text="📊 Статистика", callback_data=BroadcastAction(action="stats").pack())
    kb.button(text="🔙 Назад", callback_data=NavigationAction(action="back").pack())
    kb.adjust(2, 2, 1)
    return kb.as_markup()
```

#### 6.2. Клавиатура предварительного просмотра
```python
def broadcast_preview_keyboard() -> InlineKeyboardMarkup:
    """Клавиатура предварительного просмотра рассылки"""
    kb = InlineKeyboardBuilder()
    kb.button(text="⚙️ Настроить", callback_data=BroadcastParams(param="target_audience").pack())
    kb.button(text="📢 Разослать", callback_data=BroadcastAction(action="send_now").pack())
    kb.button(text="📝 Назад к редактированию", callback_data=BroadcastAction(action="create").pack())
    kb.adjust(2, 1)
    return kb.as_markup()
```

#### 6.3. Клавиатура настройки параметров
```python
def broadcast_params_keyboard() -> InlineKeyboardMarkup:
    """Клавиатура настройки параметров рассылки"""
    kb = InlineKeyboardBuilder()
    kb.button(text="🎯 Целевая аудитория", callback_data=BroadcastParams(param="target_audience").pack())
    kb.button(text="📅 Отложенная отправка", callback_data=BroadcastParams(param="schedule").pack())
    kb.button(text="📊 Тип рассылки", callback_data=BroadcastParams(param="broadcast_type").pack())
    kb.button(text="🔙 Назад", callback_data=BroadcastAction(action="create").pack())
    kb.adjust(2, 2)
    return kb.as_markup()
```

### 7. Улучшенная структура обработчиков

#### 7.1. Состояния FSM для рассылки
```python
class BroadcastState(StatesGroup):
    waiting_for_content = State()  # Ожидание ввода контента
    waiting_for_params = State()   # Ожидание настройки параметров
    waiting_for_confirmation = State()  # Ожидание подтверждения отправки
```

#### 7.2. Основные обработчики
- `start_broadcast_creation()` - начать создание рассылки (ожидание контента)
- `process_broadcast_content()` - обработка введенного контента
- `show_broadcast_preview()` - показ предварительного просмотра
- `show_broadcast_params()` - показ меню настройки параметров
- `set_broadcast_param()` - установка параметра рассылки
- `confirm_and_send_broadcast()` - подтверждение и отправка рассылки
### 7.2. Основные обработчики (с учетом drill-down навигации)
- `show_broadcast_history()` - показ истории рассылок (список с пагинацией)
- `show_broadcast_detail()` - показ деталей конкретной рассылки из истории
- `repeat_broadcast()` - повторная отправка выбранной рассылки
- `show_scheduled_broadcasts()` - показ отложенных рассылок (список с пагинацией)
- `show_scheduled_detail()` - показ деталей конкретной отложенной рассылки
- `send_scheduled_now()` - отправить отложенную рассылку немедленно
- `delete_scheduled_broadcast()` - удалить отложенную рассылку
- `paginate_broadcast_list()` - обработка пагинации в списках рассылок
- `edit_scheduled_broadcast()` - редактирование отложенной рассылки
- `show_broadcast_statistics()` - показ статистики по конкретной рассылке

### 9. Улучшения UX (с учетом drill-down навигации)

#### 9.1. Визуальные улучшения
- Добавить прогресс-бар при создании рассылки
- Использовать понятные иконки для каждого шага
- Показывать количество выбранных получателей в реальном времени
- Реализовать предварительный просмотр в формате, близком к реальному отображению в Telegram
- Добавить индикацию текущего уровня навигации (хлебные крошки)

#### 9.2. Функциональные улучшения
- Добавить возможность сохранения шаблонов рассылок
- Реализовать A/B тестирование для разных версий сообщений
- Добавить возможность повторного использования успешных рассылок
- Внедрить систему проверки качества контента (орфография, длина, структура)
- Добавить пагинацию в списках рассылок (Next/Prev)
- Реализовать кэширование состояний для возврата без потери контекста

#### 9.3. Навигационные улучшения (drill-down pattern)
- Добавить кнопку "Назад" на каждом уровне иерархии
- Реализовать хлебные крошки для отображения текущего уровня
- Обеспечить сохранение контекста при возврате из детального просмотра
- Добавить быстрые переходы между уровнями навигации

#### 9.4. Безопасность и ограничения
- Внедрить систему рейт-лимитов для предотвращения спама
- Добавить предупреждение при выборе слишком широкой аудитории
- Реализовать автоматическую проверку контента на наличие запрещенных слов/фраз
- Добавить логирование всех действий, связанных с рассылкой

### 10. Архитектурные улучшения

#### 10.1. Добавление модели рассылки
```python
class Broadcast(Base):
    __tablename__ = "broadcasts"
    
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
    scheduled_at: Mapped[Optional[datetime]] = mapped_column(DateTime)
    sent_at: Mapped[Optional[datetime]] = mapped_column(DateTime)
    content: Mapped[str] = mapped_column(Text)
    media_id: Mapped[Optional[str]] = mapped_column(String(255))
    target_audience: Mapped[str] = mapped_column(String(50))  # all, premium, etc.
    status: Mapped[str] = mapped_column(String(20), default="draft")  # draft, scheduled, sent, cancelled
    total_sent: Mapped[int] = mapped_column(Integer, default=0)
    total_delivered: Mapped[int] = mapped_column(Integer, default=0)
    total_opened: Mapped[int] = mapped_column(Integer, default=0)
    admin_id: Mapped[int] = mapped_column(Integer, ForeignKey("users.user_id"))
```

#### 10.2. Сервисный слой для рассылки
- `BroadcastService` - основной сервис для управления рассылками
- `BroadcastScheduler` - планировщик отложенных рассылок
- `BroadcastAnalytics` - аналитика и статистика рассылок
- `BroadcastValidator` - валидация и проверка контента

### 11. Рекомендации по реализации

1. Начать с обновления структуры callback data
2. Реализовать FSM-состояния для нового UX-потока
3. Создать новые клавиатуры с улучшенной навигацией
4. Добавить модель рассылки в базу данных
5. Реализовать сервисный слой для управления рассылками
6. Интегрировать систему аналитики
7. Добавить валидацию и безопасность
8. Тестировать UX-потоки с реальными администраторами